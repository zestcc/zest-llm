# ZestLLM 完整版推进状态

> **更新日期** 2026-06-13 · **目标** 完整版向生产签字（Linux Docker）推进

---

## 模块状态总览

| 模块 | 状态 | 验收入口 | 阻塞 |
|------|------|----------|------|
| **控制面（Admin CP）** | ✅ 功能齐备 | `full-acceptance.ps1`（**67 PASS**）· `mvn test` | 无 |
| **Demo 业务链** | ✅ methodA / flowChat / tool-loop | `demo-walkthrough.ps1` · DEMO-01/02 | Windows 需 `-WithDemo` 启动 |
| **Admin SSO** | ✅ 可插拔 SPI + Back-Channel Logout 会话吊销 | `sso-smoke.ps1` / `sso-smoke.sh` | 生产 IdP 联调待 Linux 测试机 |
| **Gitee CI** | ✅ build-and-test 自动 · docker-e2e manual | `.gitee/pipelines/zestllm-ci.yml` | 需 Linux Runner + Docker |
| **Large Tier** | ⚠️ Chart/Compose 已有；`dify-kb` 适配器已实现 | `zest-stack-up.sh large` · `validate-large-tier-compose.sh` | Dify/RAGFlow/Kafka 资源与联调 |
| **Helm Chart** | ✅ 最小可部署 + SSO env + 生产示例 values | `deploy/helm/zest-llm/` · `values-production.example.yaml` | 生产 IdP / 多 AZ 联调 |
| **生产签字** | ⏸️ 暂缓（VM 出网拉 ghcr 失败） | `run-production-signoff.sh` · Gitee `docker-e2e` | 需稳定外网或 CI Runner |
| **SSO 浏览器联调** | ✅ 清单 + 只读 DB 脚本 | `sso-local-preflight.ps1` · `sso-db-verify.ps1/.sh` | 需人工浏览器登录 + IdP |

### 本机 Linux 签字阻塞（2026-06-13 二次实测）

| 检查项 | 结果 |
|--------|------|
| `docker --version` | **未安装**（`CommandNotFoundException`） |
| `wsl --status` / `wsl -l -v` | **未安装**（提示 `wsl --install`） |
| `bash deploy/scripts/run-production-signoff.sh small` | **未执行** — 无 bash + Docker 全栈环境 |

**解除阻塞**：在 Linux 测试机或 Gitee `docker-e2e` Runner 上执行签字；本机可跑 `production-acceptance.ps1 -SkipWhiteBox` 作预检。

### CI 加固（2026-06-13）

- `docker-e2e` 在 small 栈签字前执行 `validate-large-tier-compose.sh`（compose 干跑）。
- `docker-e2e` 增加 `only: tags`，发布打 tag 后可手动触发留档。
- 归档路径：`ci-docker-e2e.log`、`validate-large-tier-compose.log`、`production-*.txt`、`signoff-*.txt`、`mvn-test-latest.log`。

### prepare 热路径（2026-06-13）

- `CachedPolicy` 缓存 `taskId`/`taskCode`，策略缓存命中时跳过 `llm_ai_task_def` 查询（生产 P95 阈值不变）。

### Helm SSO env + 本地验收（2026-06-13 晚间）

- Chart `_admin-env.tpl` 注入 `ZEST_LLM_ADMIN_SSO_*` / JWT / Redis 端口；`HelmChartAdminDeploymentTest` 静态冒烟。
- `knowledge-qa.json` 内置模板（`7eafad5`）+ AC54 对象属性校验（避免 PS5.1 中文 literal 编码问题）。
- `production-acceptance.ps1 -SkipWhiteBox`：**67 PASS / 0 FAIL / 1 SKIP**，AC54 PASS，`PhaseFailed=0`。

### VMware 签字尝试（2026-06-13，已暂缓）

| 步骤 | 结果 |
|------|------|
| SSH + Docker 启动 | ✅ MySQL `docker tag 8.0.21→8.0` 后 healthy |
| `ghcr.io/berriai/litellm` | ❌ 连接超时（DaoCloud 镜像站亦超时） |
| 结论 | 个人 VM 无稳定出网时不适合 Docker 全栈签字；改用 **Gitee docker-e2e** 或配代理后再试 |

**Compose 镜像可覆盖**（缓解 ghcr）：复制 `deploy/env.compose.example` → `deploy/.env`，或 `bash deploy/scripts/preload-stack-images.sh`。

---

## 验收链对照

| 环境 | 脚本 | 阶段 |
|------|------|------|
| Windows 本地 | `production-acceptance.ps1` | 白盒 → 黑盒 → **SSO** → 链路 → 压测 |
| Linux Docker | `production-acceptance.sh` | 白盒 → *IT → e2e → **SSO** → journeys → 压测 |
| 一键签字 | `run-production-signoff.sh` | compose up + wait + `production-acceptance.sh` |

门禁摘要：`GATE-WB` · `GATE-BB` · **`GATE-SSO`** · `GATE-CH` · `GATE-ST`

---

## Linux 测试机下一步

1. **克隆并 small 栈签字**
   ```bash
   git clone https://gitee.com/zestcc/zest-llm.git && cd zest-llm
   bash deploy/scripts/run-production-signoff.sh small
   ```
2. **确认 GATE-SSO**：`sso.enabled=false` 时 SSO 阶段 SKIP；启用 ZestSSO 后重跑 `bash deploy/scripts/sso-smoke.sh`，浏览器步骤见 `deploy/scripts/sso-browser-checklist.md`。
3. **手动触发 Gitee `docker-e2e`**，归档 `deploy/test-reports/signoff-*.txt`。
4. **（可选）medium / large**：Langfuse profile → `run-production-signoff.sh medium`；Large Tier 单独排期。
5. **Helm 试部署**：`deploy/helm/zest-llm/README.md`，核对 Ingress 与 Secret。

---

## 相关文档

| 文档 | 说明 |
|------|------|
| [ZestLLM-完整版功能清单.md](./ZestLLM-完整版功能清单.md) | 完整版定义与 API/UI 清单 |
| [ZestLLM-Admin-SSO.md](./ZestLLM-Admin-SSO.md) | SSO 配置与联调 |
| [Gitee-CI与生产签字.md](./Gitee-CI与生产签字.md) | CI 流水线与签字流程 |
| [生产级全量测试计划.md](./生产级全量测试计划.md) | 四/五阶段测试计划 |
| [deploy/scripts/sso-browser-checklist.md](../deploy/scripts/sso-browser-checklist.md) | SSO 浏览器联调 + SQL/curl |
| [Gitee-docker-e2e-触发指南.md](./Gitee-docker-e2e-触发指南.md) | 无 Linux 测试机时 CI 签字 |
| [测试执行报告-20260611.md](./测试执行报告-20260611.md) | 本地执行记录 |
