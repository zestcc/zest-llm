# ZestLLM 测试执行报告

**执行时间**：2026-06-11（首次） · **复测 2026-06-13（P1 本地完整版）** · **Demo 链复测 2026-06-13** · **完整版推进 2026-06-13** · **SSO 回退修复复测 2026-06-13** · **Back-Channel Logout / dify-kb 2026-06-13**  
**执行环境**：Windows 本地 · Admin `:8088`（嵌入 UI）· LiteLLM `:4000` mock · Demo `:8081` · ZestSSO `:9000`  
**自动化脚本**：`deploy/scripts/full-acceptance.ps1` · `production-acceptance.ps1` · `mvn test`

---

## 2026-06-13 Helm SSO env / knowledge-qa 内置模板

| 项目 | 结果 |
|------|------|
| Helm Chart | `_admin-env.tpl` 注入 `ZEST_LLM_ADMIN_SSO_*` / JWT Secret；`values.yaml` + 生产示例对齐 |
| 场景模板 | `classpath:scenario-templates/knowledge-qa.json`（与 `examples/scenarios/knowledge-qa/` 一致） |
| 验收报告编码 | `production-acceptance.ps1` / `full-acceptance.ps1` 输出 UTF-8（修复 GATE 中文乱码） |
| 单测 | `HelmChartAdminDeploymentTest` · `ScenarioTemplateServiceTest`（knowledge-qa） |
| 阻塞 | Linux Docker 生产签字 · 真实 K8s Helm 试部署 |

---

## 2026-06-13 Back-Channel Logout / dify-kb / Learning 自动发布

| 项目 | 结果 |
|------|------|
| SSO Back-Channel Logout | `ZestLlmSsoLogoutHandler` + `AdminSessionRevocationService`（Redis 吊销 JWT） |
| `mvn test -pl zest-llm-infra,zest-llm-admin -am` | **BUILD SUCCESS**（含 DifyKb / LearningAutoPublish / SSO 单测） |
| `production-acceptance.ps1 -SkipWhiteBox` | **PhaseFailed=0** · **66 PASS / 0 FAIL** 黑盒 · GATE-SSO/CH/ST 全 PASS |
| prepare P95 | **24ms**（黑盒）· 压测 P95 **365ms**（阈值 1200ms） |
| 产品缺口 | `DifyKbKnowledgeRetrievalAdapter` · `zest.llm.learning.auto-publish.enabled`（默认 false） |
| 场景模板示例 | `examples/scenarios/knowledge-qa/profile.yaml` |
| 报告 | `deploy/test-reports/production-20260613-192427.txt` |

---

## 2026-06-13 SSO 回退修复复测（Windows 无 Docker）

| 项目 | 结果 |
|------|------|
| `start-local-full.ps1` | **语法修复**（注释行与 `if` 合并导致 ParserError） |
| SSO 脚本 | `full-acceptance.ps1` / `sso-smoke.ps1` / `sso-smoke.sh` 增加 `/oidc/*` 回退 |
| `full-acceptance.ps1` | **66 PASS / 0 FAIL / 1 SKIP**（SSO-01/02/SEC-SSO 全绿） |
| `production-acceptance.ps1 -SkipWhiteBox` | **PhaseFailed=0**（GATE-BB/SSO/CH/ST/P0 全 PASS） |
| prepare P95 | **15ms**（黑盒）· 压测 P95 **502ms**（阈值 1200ms） |
| 报告 | `deploy/test-reports/production-20260613-190710.txt` |

**Linux 签字**：仍需 Docker 全栈 `bash deploy/scripts/production-acceptance.sh`（e2e AC1–38、Testcontainers `*IT`、P95≤500ms）。

---

## 2026-06-13 完整版推进

| 项目 | 结果 |
|------|------|
| Admin `:8088` | **未启动**（栈未运行） |
| `production-acceptance.ps1 -SkipWhiteBox` | **未执行**（依赖 Admin 黑盒/SSO/链路/压测） |
| `mvn test -q` | **BUILD SUCCESS**（白盒单测通过） |
| 文档/脚本 | `完整版推进状态.md` · `sso-smoke.sh` · `production-acceptance.sh` 增加 GATE-SSO |
| Flyway 最新 | **V22**（`sso_integration`） |
| 黑盒断言数 | `full-acceptance.ps1` 内 **74** 处 `Assert-Pass`（文档口径 **66+**） |

**说明**：Windows 无 Docker；Linux 测试机下一步执行 `bash deploy/scripts/run-production-signoff.sh`。

---

## 2026-06-13 Demo 链复测（无 Docker Windows）

| 项目 | 结果 |
|------|------|
| `full-acceptance.ps1` | **59 PASS / 0 FAIL / 1 SKIP**（ENV-02 UI dev 可选） |
| DEMO-01 methodA | **PASS**（traceId + answer，HTTP 200） |
| DEMO-02 flowChat | **PASS**（ZestFlow 链 + traceId + answer，AC20 冒烟） |
| `mvn test` | **BUILD SUCCESS** |
| prepare P95 | **22ms**（阈值 800ms） |
| 报告 | `deploy/test-reports/report-20260613-175514.txt` |

**一键启动（Windows 无 Docker）**：

```powershell
powershell -File deploy/scripts/start-local-full.ps1 -EmbedUi -WithLiteLLM -WithDemo -SkipBuild
powershell -File deploy/scripts/production-acceptance.ps1
```

**仍未执行**：Docker 全栈 `production-acceptance.sh`（e2e AC1–38、journeys、*IT）。

---

## 2026-06-13 生产级四阶段验收（local Tier）

| 阶段 | 结果 |
|------|------|
| 白盒 `mvn test` | BUILD SUCCESS |
| 黑盒 | **63 PASS / 0 FAIL / 1 SKIP**（含 CHAIN/REGISTRY） |
| 链路 demo-walkthrough | PASS |
| 压测 | PASS（200/200，P95=548ms ≤1200ms） |
| 主报告 | `deploy/test-reports/production-20260613-182533.txt` |

**生产签字**：Linux Docker 执行 `bash deploy/scripts/production-acceptance.sh`（P95≤500ms）。

---

## 2026-06-13 复测（P1 通过）

| 项目 | 结果 |
|------|------|
| `full-acceptance.ps1` | **57 PASS / 0 FAIL / 1 SKIP**（ENV-02 UI dev 可选） |
| `mvn test` | **BUILD SUCCESS** |
| prepare P95 | **21ms**（阈值 800ms） |
| Runtime invoke | ✅ mock LiteLLM 真链 |
| 报告 | `deploy/test-reports/report-20260613-143411.txt` |

**仍未执行**：Docker 全栈 `e2e-acceptance.sh` AC1–38、Testcontainers `*IT`（本机无 Docker）。

---

## 1. 总结（2026-06-11 首次）

### 复测（Rebuild + Flyway V17 后）— **本地 Admin 验收通过**

| 项目 | 结果 |
|------|------|
| Flyway | **V17** |
| `full-acceptance.ps1` | **43 PASS / 0 FAIL** |
| 报告 | `deploy/test-reports/report-20260611-002916.txt` |
| `meta/features` | `agentProbeApi: true`，三张探测/归档表就绪 |
| 单元测试 `*Test` | **PASS** |

### 首次执行（旧 Admin 进程）

| 类别 | 通过 | 失败 | 跳过 | 结论 |
|------|------|------|------|------|
| 单元测试（*Test） | 全部模块 | 0 | IT 需 Docker | **通过** |
| 安全 SEC-* | 4 | 0 | 0 | **通过** |
| Runtime prepare/invoke | 3 | 0 | 0 | **通过** |
| 性能 PERF-* | 2 | 0 | 0 | **通过**（P95=16ms） |
| Admin API 矩阵 | 19 | 6 | 0 | **部分失败**（旧代码） |
| 兼容性 COMPAT-* | 0 | 6 | 0 | **失败**（旧 WebMvcConfig） |
| Docker 全栈 AC1–38 | — | — | — | **未执行**（无 Docker / Demo 未起） |

**当前结论**：本地 Admin **已达验收门禁**；生产全栈仍需 Docker compose + `e2e-acceptance.sh` AC1–38。

---

## 2. 单元测试结果

```
mvn test -pl zest-llm-infra,zest-llm-agent,zest-llm-admin -am -Dtest=*Test
```

| 模块 | 结果 |
|------|------|
| zest-llm-infra | PASS |
| zest-llm-agent | PASS |
| zest-llm-admin（12 个 *Test） | PASS |

> 集成测试 `*IT`（Testcontainers）需 Docker，本机未安装 Docker，未执行。

---

## 3. 全量验收脚本结果（30 PASS / 13 FAIL）

报告文件：`deploy/test-reports/report-20260611-002413.txt`

### 3.1 通过项（节选）

| ID | 说明 |
|----|------|
| SEC-01~04 | 错误 Token、未登录拦截、OPERATOR 只读/禁止 probe-all |
| RT-01~03 | prepare、invoke、Execution 审计 |
| PROBE-01 | 探测执行并落库 |
| PERF-01~02 | prepare P50=12ms P95=16ms，成功率 100% |
| API-06~10,12,14~15,17~25 | 观测、Execution、Profile、探测历史、运维列表等 |

### 3.2 失败项与根因

| ID | 现象 | 根因 | 修复动作 |
|----|------|------|----------|
| API-02 | meta/features 404 | 运行中 Admin **非最新构建** | IDEA Rebuild + 重启，确认含 `AdminMetaController` |
| API-03/04 | dashboard 500 | `agent-health` 聚合 SQL/旧代码 | 已优化 `MAX(id)` 聚合；重启后复测 |
| API-11 | trend 404 | 同上，新接口未加载 | 重启最新代码 |
| API-13 | cost summary 404 | 同上 | 重启最新代码 |
| API-16 | archive runs 404 | Flyway V17 未执行或旧代码 | 执行 V17 + 重启 |
| COMPAT-* | SPA 路由 | 旧 WebMvcConfig 无 forward | 部署 `6b3f2d1+` 后复测 |

### 3.3 跳过项（环境缺失）

| 依赖 | 影响用例 |
|------|----------|
| Demo `:8081` | AC1、AC20、demo flowChat |
| LiteLLM `:4000` | 网关冒烟、AC21 真链 |
| Docker Compose | AC1–38 完整 bash E2E、Testcontainers IT |

---

## 4. 性能测试

| 指标 | 阈值（本地脚本） | 实测 | 结果 |
|------|------------------|------|------|
| prepare P95 | ≤800ms | **16ms** | PASS |
| 成功率 | ≥90% | **100%**（30/30） | PASS |

生产建议：在 Docker 全栈下运行 `loadtest-cp-prepare.sh`，门禁 **P95≤500ms**。

---

## 5. 安全测试

| 用例 | 结果 |
|------|------|
| 错误 Bearer Token → AUTH_FAILED | PASS |
| 未登录访问 `/api/admin/**` → 403 | PASS |
| OPERATOR GET 任务列表 → 200 | PASS |
| OPERATOR POST probe-all → 403 | PASS |

---

## 6. 缺陷清单

| 级别 | ID | 描述 | 状态 |
|------|-----|------|------|
| S2 | BUG-001 | Dashboard stats/agent-health 500 | **已关闭**（2026-06-13 复测 58 PASS） |
| S2 | BUG-002 | 新 API 404（meta/trend/summary/archive-runs） | **已关闭**（2026-06-13 复测通过） |
| S3 | BUG-003 | SPA 深链 forward 缺失 | **已关闭**（WebMvcConfig 已修复并验证） |
| S2 | ENV-001 | 本机无 Docker，无法跑 AC1–38 全栈 | 开放（需 `docker compose up` 或 Gitee manual e2e） |

---

## 7. 回归与上线门禁

上线前必须满足：

1. `mvn test` 全绿（含 Docker 时跑 `*IT`）
2. `bash deploy/scripts/e2e-acceptance.sh` AC1–38 全 PASS
3. `full-acceptance.ps1` 或等价脚本 **0 FAIL**
4. `GET /api/admin/meta/features` → `agentProbeApi: true`
5. Flyway ≥ **V17**

---

## 8. 下一步操作（建议顺序）

```powershell
# 1. 拉取最新代码（含本报告修复）
git pull

# 2. 重启 Admin（local profile，确认 Flyway V17）
# IDEA: Rebuild Project → Run ZestLlmAdminApplication

# 3. 复跑验收
powershell -ExecutionPolicy Bypass -File deploy/scripts/full-acceptance.ps1

# 4. Docker 全栈（生产级）
cd deploy && docker compose up -d
bash deploy/scripts/e2e-acceptance.sh
bash deploy/scripts/run-journeys.sh
```

详细计划见 [生产级全量测试计划.md](./生产级全量测试计划.md)。
